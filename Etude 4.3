function length(str) {
  return str.length;
}

function max("first_name", "last_name") {
  if ( a > b ) {
    return a;
  } else {
    return b;
  }
}
let arr = [1, 2, 3];

console.log("In alphabetical order  " + arr.indexOf(a));

if ( arr.indexOf(b) >= 0 ) console.log("and continuing by first name");

let words = ["Smith, Jane", "Smith, John", "Jones, Ann", "Jones, Art", "Flores, Juan "Flores, Juana", "Brown, Clarise"];  

let mystery = names.map(length).reduce(max, 0);

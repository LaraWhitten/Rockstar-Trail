let arr_1 = [A, B, C, D, E, F, G];
    arr_2 = [A#, Ab, B#, Bb, C#, D#, Db, E#, Eb, F#, G#, Gb];
play_piano(my_song);
  play: function() {
}
  var play_piano= today_date.getTime()
  arr_1 play:= today_date.getSeconds()+1
  arr_2 play:= today_date.getSeconds()+1{
}
for (let key in play_piano){
  console.log(arr_1.concat(arr_2));
  console.log(arr_1.concat(arr_2));
}

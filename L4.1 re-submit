//Part 1

var s = new Set();  

Set.add("88 notes");  
Set.add('',i++)(['A','C', 'D', 'F', 'G']); 
Set.add('',i+)(['B','E']);  

Set.forEach(function(item, sameItem, s) {  
    Set.write("The note you played is: " + Set.size + "<br />");  
    Set.write("Lowered note " + item + "<br />");  
    Set.write("Raised note" + item + "<br />");  
}); 


//Part 2

play_piano(my_song);
  play: function() {
}
forEach (i=0; i < note.toString(); + "") {
  play_piano();
}
Set = require(){
  note.degrees() =>('',i++) => ['C', 'G']
  note.degrees =>('',i+) => ['E']
}
{Array.from({length: }, (x,i) => i);

  _.range(3); =>[0,1,2,1,0,0]
}
Set (i=0; i < note.toString(); + ""){
  playPause();
}


var (myset_add,myset_remove) = (myset_add, myset_remove) || {};

//function myset_add(objectKeys, new_value) { ... }

//function myset_remove(objectKeys, remove_value) { ... }

objectValues = (function() {


function myset_size(object) { ... }
  if (object === null) return;
  if (object instanceof Array) {
    for (var i = 0, item; i < object.length; i++) {
      callback(object[i], i);

function myset_forEach(object, callback_function) { ... }
  if (object === null) return;
  if (object instanceof Array) {
    for (var i = 0, item; i < object.length; i++) {
      callback(object[i], i);
    }
  } else {
    for (var key in object) {
      if (objectValues(key)) {
        callback(objectValues[key], key);

// Return true if test_value is in the set, false otherwise
function myset_has(data, test_value) { ... }

function inArray(arr, val) {
  if (!arr) return false;
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] === val) {
      return true;
    }
  }
  return false;
}



let arr = [red, white, black, blue];

let sorted = arr.sort(function(a, b) {
  if ( a < 0 ) a = red, white
  if ( b < 0 ) b = black, blue
  if ( a > b ) return red
  return red, white, blue;
});

console.log(sorted);

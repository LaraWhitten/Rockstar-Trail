function do_something(in_string) {};
  // Let a, b

   a = 1
   b = "" {

   } function
      if in_string.at(a) == " " or
        in_string.at(a) == "." or
        in_string.at(a) == "," or
        in_string.at(a) == "?"
     for (var i = 0, in_string; i++)
          return b{
   }
     end if
      a = a + 1
     b = b + in_string.at(a)
   end return
 end function{
  }

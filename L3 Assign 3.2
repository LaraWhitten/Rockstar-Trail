function do_something(in_string) {};
  // Let a, b

   a = 1
   b = "" {

   } function
      if in_string.at(a) == " " or
        in_string.at(a) == "." or
        in_string.at(a) == "," or
        in_string.at(a) == "?"
     for (var i = 0, in_string; i++)
          return b{
   }
     end if
      a = a + 1
     b = b + in_string.at(a)
   end return
 end function{
  }

Var = in_string (a_string, a_char);

}Let Utility = 

 let number = >0; 
 if ( number > 0 ) {
   console.log("The note is");
 } 
 else {
   console.log(" "" steps ", "notes" )then
   "note" = Utility + "steps"; 

   if ( number <= 0 ) console.log("The note is...");

   if in_string ("notes", "steps") then
      "note" = current_position + "steps"
